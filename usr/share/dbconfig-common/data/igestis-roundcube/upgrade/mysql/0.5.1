

-- Updates from version 0.4.2

ALTER TABLE `ROUNDCUBE_users` DROP INDEX `username_index`;
ALTER TABLE `ROUNDCUBE_users` ADD UNIQUE `username` (`username`, `mail_host`);

ALTER TABLE `ROUDCUBE_contacts` MODIFY `email` varchar(255) NOT NULL;

TRUNCATE TABLE `ROUNDCUBE_messages`;

